#!/usr/bin/env python3
"""
Example hook that normalises task names before creation.
"""

import json
import sys

payload = json.load(sys.stdin)
payload["name"] = payload["name"].strip().title()
json.dump(payload, sys.stdout)
